# 管理员科室、医生前端实现说明

## 1. 目标

为管理端提供两块页面：

1. 科室管理
2. 医生管理

当前后端这两块接口已经支持以下角色访问：

- `ADMIN`
- `MANAGEMENT`
- `REGISTRATION_CLERK`

如果你的前端只想给管理员页开放给管理员相关角色，建议管理端菜单至少放行：

- `ADMIN`
- `MANAGEMENT`

对应后端接口位置：

- 科室管理接口：[doctor-service/src/main/java/com/neusoft/neu23/neuhospital/doctor/controller/DepartmentController.java](/Users/leon/Desktop/code/javaProject/neuhospital_2/doctor-service/src/main/java/com/neusoft/neu23/neuhospital/doctor/controller/DepartmentController.java:23)
- 医生管理接口：[doctor-service/src/main/java/com/neusoft/neu23/neuhospital/doctor/controller/DoctorController.java](/Users/leon/Desktop/code/javaProject/neuhospital_2/doctor-service/src/main/java/com/neusoft/neu23/neuhospital/doctor/controller/DoctorController.java:31)

## 2. 前端菜单与路由建议

建议新增两个菜单：

- `科室管理`，路由 `/admin/departments`
- `医生管理`，路由 `/admin/doctors`

建议前端路由守卫判断：

```ts
const ADMIN_ROLES = ["ADMIN", "MANAGEMENT"];

export function canAccessAdminPage(userRole?: string) {
  return !!userRole && ADMIN_ROLES.includes(userRole);
}
```

如果你的登录态里返回的是角色数组，就改成：

```ts
const ADMIN_ROLES = ["ADMIN", "MANAGEMENT"];

export function canAccessAdminPage(userRoles: string[] = []) {
  return userRoles.some((role) => ADMIN_ROLES.includes(role));
}
```

## 3. 科室管理接口

### 3.1 查询列表

`GET /api/departments`

返回字段：

- `id`
- `deptCode`
- `deptName`
- `deptType`
- `description`
- `status`

### 3.2 查询详情

`GET /api/departments/{id}`

### 3.3 新增科室

`POST /api/departments`

请求体：

```json
{
  "deptCode": "CARDIO",
  "deptName": "心内科",
  "deptType": "CLINICAL",
  "description": "负责心血管疾病门诊和住院诊疗"
}
```

### 3.4 更新科室

`PUT /api/departments/{id}`

请求体：

```json
{
  "deptName": "心血管内科",
  "deptType": "CLINICAL",
  "description": "更新后的描述",
  "status": "ENABLED"
}
```

注意：

- 后端目前没有删除接口
- 前端建议用“停用/启用”代替删除
- 列表接口当前只返回 `status = ENABLED` 的科室，停用后可能不会继续出现在列表里

## 4. 医生管理接口

### 4.1 分页查询

`GET /api/doctors/page?pageNo=1&pageSize=10&departmentId=1&keyword=张`

返回字段：

- `records`
- `current`
- `size`
- `total`

每条记录字段：

- `id`
- `doctorNo`
- `name`
- `gender`
- `title`
- `departmentId`
- `departmentName`
- `introduction`
- `specialty`
- `phone`
- `status`

### 4.2 查询详情

`GET /api/doctors/{id}`

### 4.3 新增医生

`POST /api/doctors`

请求体：

```json
{
  "name": "李华",
  "gender": "男",
  "title": "主任医师",
  "departmentId": 9101,
  "introduction": "从事心血管疾病诊疗十余年",
  "specialty": "冠心病、高血压",
  "phone": "13800138000"
}
```

说明：

- 新建医生时后端会自动生成 `doctorNo`
- 后端会自动创建对应登录账号
- 默认密码是 `123456`

### 4.4 更新医生

`PUT /api/doctors/{id}`

请求体：

```json
{
  "name": "李华",
  "title": "副主任医师",
  "departmentId": 9101,
  "introduction": "更新后的简介",
  "specialty": "心律失常",
  "phone": "13800138001",
  "status": "ENABLED"
}
```

### 4.5 非分页列表

`GET /api/doctors?departmentId=9101`

这个接口适合做下拉框，不适合管理表格主列表。

## 5. 页面设计建议

## 5.1 科室管理页

页面结构建议：

1. 顶部筛选区
2. 表格区
3. 新增/编辑弹窗

表格列建议：

- 科室编码 `deptCode`
- 科室名称 `deptName`
- 科室类型 `deptType`
- 描述 `description`
- 状态 `status`
- 操作

操作按钮建议：

- 编辑
- 停用
- 启用

由于后端没有真正删除，建议不要放“删除”按钮。

### 科室表单字段

- 科室编码
- 科室名称
- 科室类型
- 描述

编辑时额外展示：

- 状态

### 科室前端校验建议

- `deptCode` 必填，建议只允许大写字母、数字、下划线、中划线
- `deptName` 必填
- `deptType` 必填

## 5.2 医生管理页

页面结构建议：

1. 顶部筛选区
2. 分页表格
3. 新增/编辑弹窗

筛选项建议：

- 科室
- 关键字

表格列建议：

- 医生工号 `doctorNo`
- 姓名 `name`
- 性别 `gender`
- 职称 `title`
- 所属科室 `departmentName`
- 擅长方向 `specialty`
- 联系电话 `phone`
- 状态 `status`
- 操作

操作按钮建议：

- 编辑
- 查看详情
- 停用
- 启用

### 医生表单字段

- 姓名
- 性别
- 职称
- 所属科室
- 医生简介
- 擅长方向
- 联系电话

编辑时额外展示：

- 状态

### 医生前端校验建议

- `name` 必填
- `departmentId` 必填
- `phone` 建议校验手机号格式
- `title` 建议必填

## 6. 前端 API 封装示例

下面示例按常见 `axios` 风格写。

```ts
import request from "@/utils/request";

export function getDepartments() {
  return request.get("/api/departments");
}

export function getDepartmentDetail(id: number) {
  return request.get(`/api/departments/${id}`);
}

export function createDepartment(data: {
  deptCode: string;
  deptName: string;
  deptType: string;
  description?: string;
}) {
  return request.post("/api/departments", data);
}

export function updateDepartment(
  id: number,
  data: {
    deptName?: string;
    deptType?: string;
    description?: string;
    status?: string;
  }
) {
  return request.put(`/api/departments/${id}`, data);
}

export function getDoctorsPage(params: {
  pageNo: number;
  pageSize: number;
  departmentId?: number;
  keyword?: string;
}) {
  return request.get("/api/doctors/page", { params });
}

export function getDoctorDetail(id: number) {
  return request.get(`/api/doctors/${id}`);
}

export function createDoctor(data: {
  name: string;
  gender?: string;
  title?: string;
  departmentId: number;
  introduction?: string;
  specialty?: string;
  phone?: string;
}) {
  return request.post("/api/doctors", data);
}

export function updateDoctor(
  id: number,
  data: {
    name?: string;
    title?: string;
    departmentId?: number;
    introduction?: string;
    specialty?: string;
    phone?: string;
    status?: string;
  }
) {
  return request.put(`/api/doctors/${id}`, data);
}
```

## 7. 页面状态流转建议

### 科室管理

1. 页面初始化调用 `getDepartments`
2. 点击新增，打开新增弹窗
3. 提交成功后关闭弹窗并刷新列表
4. 点击编辑，先查详情或直接回填当前行
5. 点击停用，本质调用 `updateDepartment(id, { status: "DISABLED" })`
6. 点击启用，本质调用 `updateDepartment(id, { status: "ENABLED" })`

### 医生管理

1. 页面初始化先调用 `getDepartments`
2. 再调用 `getDoctorsPage`
3. 新增和编辑都复用一个弹窗
4. 提交成功后刷新当前页
5. 点击停用，本质调用 `updateDoctor(id, { status: "DISABLED" })`
6. 点击启用，本质调用 `updateDoctor(id, { status: "ENABLED" })`

## 8. Vue 3 页面骨架示例

如果你前端是 Vue 3，可以按这个结构拆文件：

```text
src/views/admin/departments/index.vue
src/views/admin/doctors/index.vue
src/api/department.ts
src/api/doctor.ts
```

### `src/views/admin/departments/index.vue`

```vue
<script setup lang="ts">
import { onMounted, ref } from "vue";
import { createDepartment, getDepartments, updateDepartment } from "@/api/department";

const loading = ref(false);
const list = ref<any[]>([]);
const dialogVisible = ref(false);
const editingId = ref<number | null>(null);
const form = ref({
  deptCode: "",
  deptName: "",
  deptType: "",
  description: "",
  status: "ENABLED"
});

async function loadData() {
  loading.value = true;
  try {
    const res = await getDepartments();
    list.value = res.data?.data || [];
  } finally {
    loading.value = false;
  }
}

function openCreate() {
  editingId.value = null;
  form.value = {
    deptCode: "",
    deptName: "",
    deptType: "",
    description: "",
    status: "ENABLED"
  };
  dialogVisible.value = true;
}

function openEdit(row: any) {
  editingId.value = row.id;
  form.value = {
    deptCode: row.deptCode,
    deptName: row.deptName,
    deptType: row.deptType,
    description: row.description || "",
    status: row.status || "ENABLED"
  };
  dialogVisible.value = true;
}

async function submit() {
  if (editingId.value) {
    await updateDepartment(editingId.value, {
      deptName: form.value.deptName,
      deptType: form.value.deptType,
      description: form.value.description,
      status: form.value.status
    });
  } else {
    await createDepartment({
      deptCode: form.value.deptCode,
      deptName: form.value.deptName,
      deptType: form.value.deptType,
      description: form.value.description
    });
  }
  dialogVisible.value = false;
  await loadData();
}

onMounted(loadData);
</script>
```

### `src/views/admin/doctors/index.vue`

```vue
<script setup lang="ts">
import { onMounted, ref } from "vue";
import { getDepartments } from "@/api/department";
import { createDoctor, getDoctorDetail, getDoctorsPage, updateDoctor } from "@/api/doctor";

const loading = ref(false);
const list = ref<any[]>([]);
const departments = ref<any[]>([]);
const total = ref(0);
const query = ref({
  pageNo: 1,
  pageSize: 10,
  departmentId: undefined as number | undefined,
  keyword: ""
});

const dialogVisible = ref(false);
const editingId = ref<number | null>(null);
const form = ref({
  name: "",
  gender: "",
  title: "",
  departmentId: undefined as number | undefined,
  introduction: "",
  specialty: "",
  phone: "",
  status: "ENABLED"
});

async function loadDepartments() {
  const res = await getDepartments();
  departments.value = res.data?.data || [];
}

async function loadDoctors() {
  loading.value = true;
  try {
    const res = await getDoctorsPage(query.value);
    const page = res.data?.data;
    list.value = page?.records || [];
    total.value = page?.total || 0;
  } finally {
    loading.value = false;
  }
}

function openCreate() {
  editingId.value = null;
  form.value = {
    name: "",
    gender: "",
    title: "",
    departmentId: undefined,
    introduction: "",
    specialty: "",
    phone: "",
    status: "ENABLED"
  };
  dialogVisible.value = true;
}

async function openEdit(row: any) {
  editingId.value = row.id;
  const res = await getDoctorDetail(row.id);
  const detail = res.data?.data;
  form.value = {
    name: detail.name || "",
    gender: detail.gender || "",
    title: detail.title || "",
    departmentId: detail.departmentId,
    introduction: detail.introduction || "",
    specialty: detail.specialty || "",
    phone: detail.phone || "",
    status: detail.status || "ENABLED"
  };
  dialogVisible.value = true;
}

async function submit() {
  if (editingId.value) {
    await updateDoctor(editingId.value, form.value);
  } else {
    await createDoctor(form.value);
  }
  dialogVisible.value = false;
  await loadDoctors();
}

onMounted(async () => {
  await loadDepartments();
  await loadDoctors();
});
</script>
```

## 9. 权限控制建议

前端按钮级权限建议：

- `ADMIN`、`MANAGEMENT`：显示科室/医生管理入口
- `REGISTRATION_CLERK`：如果你的业务也允许挂号员管理建档，可显示新增和编辑按钮
- 如果管理端只想给管理员用，就只在前端展示给 `ADMIN`、`MANAGEMENT`

前端建议做两层保护：

1. 路由守卫控制页面访问
2. 按钮级权限控制新增、编辑、启停按钮

## 10. 已知限制

当前后端接口有这些特点，前端实现时要注意：

1. 科室和医生都没有物理删除接口
2. 科室列表接口只返回启用状态科室
3. 医生分页支持 `departmentId` 和 `keyword`
4. 医生新增后会自动创建账号，默认密码 `123456`
5. 医生更新接口支持修改 `status`

## 11. 交付建议

如果你让前端同学直接开工，最小交付顺序建议：

1. 先做科室管理页
2. 再做医生管理页
3. 最后补路由权限、按钮权限和状态启停

如果你需要，我下一步可以继续直接给你补一版：

- `Vue3 + Element Plus` 的完整页面代码
- 或者 `React + Ant Design` 的完整页面代码
