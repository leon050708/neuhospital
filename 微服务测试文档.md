# 微服务测试文档

本文档是当前微服务架构的统一测试说明，整合自原有测试说明和第三阶段测试进度记录。当前结论：微服务架构已经成型，并在默认约束下可用；`payment-service` 暂不切流，支付接口继续由 `backend-service` 承载。

## 1. 测试环境

| 组件 | 地址或端口 | 用途 |
| --- | --- | --- |
| Nacos 控制台 | `http://localhost:8080` | 查看服务注册情况 |
| Nacos 注册发现 | `127.0.0.1:8848` | Spring Cloud Alibaba 客户端连接 |
| Redis | `localhost:6380` | 网关限流、挂号限流、Redisson 锁 |
| Kafka | `localhost:9094` | 挂号本地消息表投递消费 |
| MinIO | `localhost:9005` | 文件上传存储 |
| PostgreSQL | 远程测试库 | 项目业务数据 |

## 2. 测试脚本

| 脚本 | 覆盖范围 | 当前结果 |
| --- | --- | --- |
| `scripts/check-gateway.sh` | 网关健康、Nacos、OpenAPI、未登录鉴权 | 可用于基础检查 |
| `scripts/test-microservices.sh` | 网关、认证、患者、文件、后端回退、医生科室 | 通过 |
| `scripts/test-registration-service.sh` | 挂号服务健康、Nacos、直连读接口、网关读接口 | 通过 |
| `scripts/test-second-phase-services.sh` | 药房、检查检验处置、门诊服务 Nacos 和网关读接口 | 通过 |

推荐测试顺序：

```bash
cd /Users/aijiaqi/shixun_2026summer/hou1
./scripts/start-microservices.sh
./scripts/test-microservices.sh
./scripts/test-registration-service.sh
./scripts/test-second-phase-services.sh
```

## 3. 最终联调结果

2026-07-05 已完成三组核心测试：

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| 一键启动 | 通过 | 默认服务健康检查均达到 UP，`payment-service` 跳过 |
| 基础微服务链路 | 通过 | 注册、登录、患者、文件、医生科室、后端回退均通过 |
| 挂号服务链路 | 通过 | 健康检查、Nacos、直连读接口、网关读接口均通过 |
| 二阶段服务链路 | 通过 | 药房、检查、门诊经网关读接口通过 |

二阶段脚本第一次在全部服务同时运行时出现过 `CannotGetJdbcConnectionException`。停止本轮不需要的 `file-service`、`patient-service`、`doctor-service`、`registration-service` 后重跑通过。该现象归类为共享远程 PostgreSQL 连接容量限制，不归类为微服务拆分逻辑失败。

## 4. 编译与一致性验证

已执行过的验证命令：

```bash
./mvnw -q -DskipTests validate
./mvnw -q -DskipTests compile
./mvnw -q -pl payment-service,pharmacy-service,inspection-service,outpatient-service test
```

药房、检查检验处置、门诊服务与单体对应业务包做过逐文件比对：

```bash
diff -qr backend-service/src/main/java/com/neusoft/neu23/neuhospital/pharmacy pharmacy-service/src/main/java/com/neusoft/neu23/neuhospital/pharmacy
diff -qr backend-service/src/main/java/com/neusoft/neu23/neuhospital/inspection inspection-service/src/main/java/com/neusoft/neu23/neuhospital/inspection
diff -qr backend-service/src/main/java/com/neusoft/neu23/neuhospital/outpatient outpatient-service/src/main/java/com/neusoft/neu23/neuhospital/outpatient
```

比对无输出，表示当前独立服务中的业务包与单体对应包保持一致。

## 5. 各服务测试结果

### 网关与认证

| 测试项 | 路径 | 结果 |
| --- | --- | --- |
| 网关健康 | `GET /actuator/health` | 通过 |
| Nacos 服务列表 | Nacos OpenAPI | 通过 |
| 登录 | `POST /api/auth/login` | 通过 |
| 注册 | `POST /api/auth/register` | 通过 |
| 当前用户 | `GET /api/auth/me` | 通过 |
| 后端文档回退 | `GET /v3/api-docs` | 通过 |

### patient-service

| 测试项 | 路径 | 结果 |
| --- | --- | --- |
| Nacos 注册 | `patient-service` | 通过 |
| 患者详情查询 | `GET /api/patients/{bizId}` | 通过 |
| 经网关访问患者详情 | `/api/patients/**` | 通过 |

未逐项回归患者完整增删改查和患者建档同步账号的全部异常场景。

### file-service

| 测试项 | 路径 | 结果 |
| --- | --- | --- |
| Nacos 注册 | `file-service` | 通过 |
| 文件上传 | `POST /api/files/upload` | 通过 |
| 文件详情查询 | `GET /api/files/{fileId}` | 通过 |
| 经网关访问文件接口 | `/api/files/**` | 通过 |

未专项测试大文件、下载、预览、删除、MinIO 异常和重复 objectKey。

### doctor-service

| 测试项 | 路径 | 结果 |
| --- | --- | --- |
| Nacos 注册 | `doctor-service` | 通过 |
| 科室列表 | `GET /api/departments` | 通过 |
| 医生分页 | `GET /api/doctors/page?pageNo=1&pageSize=2` | 通过 |
| 经网关访问科室 | `/api/departments/**` | 通过 |
| 经网关访问医生 | `/api/doctors/**` | 通过 |

医生分页已返回 `pageSize=2`、`total=10`，分页插件修复生效。

已知历史问题：医生更新接口曾出现 `JDBC rollback failed` 或超时，本轮不作为微服务拆分通过标准。

### registration-service

| 测试项 | 路径或动作 | 结果 |
| --- | --- | --- |
| 服务启动 | `registration-service:10023` | 通过 |
| Nacos 注册 | `registration-service` | 通过 |
| Redis 连接 | `localhost:6380` | 通过 |
| Kafka 消费者启动 | `topic_registration_create` | 通过 |
| 健康检查 | `GET /actuator/health` | 通过 |
| 排班列表 | `GET /api/schedules` | 通过 |
| 我的挂号 | `GET /api/registrations/my` | 通过 |
| 经网关排班查询 | `GET /api/schedules` | 通过 |
| 经网关我的挂号 | `GET /api/registrations/my` | 通过 |

本轮不把 `/api/registrations/quick` 快速挂号作为通过标准。该链路依赖 Redis Lua、Redisson 锁、Kafka、本地消息表、数据库库存扣减和支付状态联动，需要后续专项测试。

### payment-service

| 测试项 | 结果 |
| --- | --- |
| 独立编译 | 通过 |
| 聚合编译 | 通过 |
| 网关默认路由 | `/api/payment/** -> backend-service` |

暂未测试独立支付服务承接网关正式流量。原因是支付成功和支付超时仍依赖同 JVM Spring 事件，跨服务后需要改造为 Kafka 或显式服务间接口。

### pharmacy-service

| 测试项 | 路径或动作 | 结果 |
| --- | --- | --- |
| 独立编译 | `cd pharmacy-service && ../mvnw -q -DskipTests compile` | 通过 |
| 与单体功能包比对 | `diff -qr .../pharmacy .../pharmacy` | 无差异 |
| 网关路由配置 | 药房路径 -> `lb://pharmacy-service` | 通过 |
| 网关读接口烟测 | `/api/drugs`, `/api/prescriptions` | 通过 |

支付成功和支付超时后自动更新处方状态未测，等待支付事件改造。

### inspection-service

| 测试项 | 路径或动作 | 结果 |
| --- | --- | --- |
| 独立编译 | `cd inspection-service && ../mvnw -q -DskipTests compile` | 通过 |
| 与单体功能包比对 | `diff -qr .../inspection .../inspection` | 无差异 |
| 网关路由配置 | 检查检验处置路径 -> `lb://inspection-service` | 通过 |
| 网关读接口烟测 | 检查、检验、处置分页查询 | 通过 |

`/api/ct-analysis/**` 仍由 `backend-service` 提供，不纳入本轮 `inspection-service` 验收。

### outpatient-service

| 测试项 | 路径或动作 | 结果 |
| --- | --- | --- |
| 独立编译 | `cd outpatient-service && ../mvnw -q -DskipTests compile` | 通过 |
| 与单体功能包比对 | `diff -qr .../outpatient .../outpatient` | 无差异 |
| 网关路由配置 | `/api/outpatient/** -> lb://outpatient-service` | 通过 |
| 网关读接口烟测 | `/api/outpatient/records` | 通过 |

门诊新增、修改、删除和复杂跨服务展示未逐项回归。

## 6. 当前没有独立验证的业务域

| 业务域 | 当前状态 |
| --- | --- |
| 系统管理 | 仍在 `backend-service` |
| CT 分析 | 仍在 `backend-service` |
| AI | 仍在 `backend-service` |

## 7. 已知限制

1. `payment-service` 暂不切流。
2. 快速挂号 `/api/registrations/quick` 需要专项测试。
3. 远程 PostgreSQL 连接容量有限，全服务同时运行时可能出现连接获取失败。
4. 医生更新接口历史上出现过数据库回滚或超时问题。
5. 患者、药房、检查、门诊等服务未做完整增删改查异常路径回归。

## 8. 总体结论

当前已经验证通过的主链路：

```text
gateway-service
  -> backend-service      认证、系统、支付、CT、AI、后端回退
  -> patient-service      患者详情
  -> file-service         文件上传和查询
  -> doctor-service       医生科室查询
  -> registration-service 排班和挂号查询
  -> pharmacy-service     药品和处方读接口
  -> inspection-service   检查、检验、处置读接口
  -> outpatient-service   门诊病历读接口
```

因此，当前微服务架构已经基本成型，并且可以作为本项目实习成果的微服务版本使用。需要在说明中明确：支付微服务暂缓切流，数据库连接容量限制属于当前测试环境约束。

