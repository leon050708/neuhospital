# 微服务启动文档

本文档是当前微服务架构的统一启动入口，整合自原有网关启动说明、一键启动说明和阶段测试记录。当前项目以实习成果演示为目标，默认启动已经验证通过的服务；`payment-service` 暂不切流，支付接口继续由 `backend-service` 承载。

## 1. 服务与端口

| 服务 | 服务名 | 端口 | 默认启动 | 说明 |
| --- | --- | --- | --- | --- |
| 原后端 | `backend-service` | `8081` | 是 | 认证、系统、支付、CT 分析、AI 和兜底接口 |
| 网关 | `gateway-service` | `10010` | 是 | 前端和接口工具统一入口 |
| 文件服务 | `file-service` | `10020` | 是 | `/api/files/**` |
| 患者服务 | `patient-service` | `10021` | 是 | `/api/patients/**` |
| 医生科室服务 | `doctor-service` | `10022` | 是 | `/api/doctors/**`, `/api/departments/**` |
| 排班挂号队列服务 | `registration-service` | `10023` | 是 | `/api/schedules/**`, `/api/registrations/**`, `/api/queue/**` |
| 支付服务 | `payment-service` | `10024` | 否 | 已拆分但默认不承接网关流量 |
| 药房服务 | `pharmacy-service` | `10025` | 是 | `/api/drugs/**`, `/api/prescriptions/**`, `/api/pharmacy/**` |
| 检查检验处置服务 | `inspection-service` | `10026` | 是 | 检查、检验、处置接口 |
| 门诊服务 | `outpatient-service` | `10027` | 是 | `/api/outpatient/**` |

## 2. 前置条件

本地需要先准备：

| 组件 | 地址或端口 | 说明 |
| --- | --- | --- |
| Nacos 控制台 | `http://localhost:8080` | 浏览器查看服务注册 |
| Nacos 注册发现 | `127.0.0.1:8848` | Java 客户端连接地址 |
| Redis | `localhost:6380` | 网关限流、挂号限流、Redisson 锁 |
| Kafka | `localhost:9094` | 挂号本地消息表投递消费 |
| MinIO | `localhost:9005` | 文件上传存储 |
| PostgreSQL | 远程测试库 | 项目业务数据 |

注意：浏览器访问 Nacos 用 `8080`，Java 服务注册使用 `8848`。不要把 `NACOS_SERVER_ADDR` 写成 `127.0.0.1:8080`。

## 3. 一键启动

推荐从项目根目录执行：

```bash
cd /Users/aijiaqi/shixun_2026summer/hou1
./scripts/start-microservices.sh
```

脚本会自动完成：

1. 启动 Redis、Kafka、MinIO。
2. 检查本地 Nacos 是否可访问。
3. 先启动 `backend-service` 并等待健康检查通过。
4. 启动其他默认微服务。
5. 跳过 `payment-service`。
6. 启动 `gateway-service`。
7. 等待所有默认服务 `/actuator/health` 返回可用。

脚本默认降低数据库连接池大小：

```text
SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE=2
SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE=1
```

这是为了减少远程共享 PostgreSQL 出现连接数不足的概率。

## 4. 启动成功检查

网关健康检查：

```bash
curl -fsS http://127.0.0.1:10010/actuator/health
```

Nacos 服务列表：

```bash
curl -fsS "http://127.0.0.1:8848/nacos/v1/ns/service/list?pageNo=1&pageSize=20"
```

默认应看到：

```text
backend-service
gateway-service
file-service
patient-service
doctor-service
registration-service
pharmacy-service
inspection-service
outpatient-service
```

`payment-service` 只有在专项验证时才要求出现在 Nacos 中。

## 5. 访问入口

前端和接口工具优先访问网关：

| 功能 | 地址 |
| --- | --- |
| 登录 | `POST http://localhost:10010/api/auth/login` |
| 注册 | `POST http://localhost:10010/api/auth/register` |
| 当前用户 | `GET http://localhost:10010/api/auth/me` |
| OpenAPI | `http://localhost:10010/v3/api-docs` |
| 网关健康 | `http://localhost:10010/actuator/health` |

除登录、注册、刷新、登出、文档和健康检查外，其他接口需要：

```text
Authorization: Bearer <accessToken>
```

## 6. 停止服务

停止 Java 微服务：

```bash
cd /Users/aijiaqi/shixun_2026summer/hou1
./scripts/stop-microservices.sh
```

停止 Redis、Kafka、MinIO：

```bash
docker compose -f infra/compose.yml stop redis kafka minio
```

## 7. 日志位置

一键启动脚本日志在：

```text
/Users/aijiaqi/shixun_2026summer/hou1/logs/microservices
```

常用查看命令：

```bash
tail -f logs/microservices/backend-service.log
tail -f logs/microservices/gateway-service.log
tail -f logs/microservices/pharmacy-service.log
```

## 8. 支付服务专项启动

默认演示链路不启动独立支付服务。原因是当前支付成功和支付超时仍依赖同 JVM Spring 事件通知挂号、检查检验、药房等业务，拆成独立 JVM 后事件不会自然跨服务传播。

如果只是验证 `payment-service` 可独立启动，可以执行：

```bash
START_PAYMENT_SERVICE=true PAYMENT_SERVICE_URI=lb://payment-service ./scripts/start-microservices.sh
```

专项验证结束后，正式演示仍建议恢复默认配置，让 `/api/payment/**` 走 `backend-service`。

## 9. 常见问题

### Nacos 9848 端口被占用

当前流程使用本地已有 Nacos，不需要额外启动 Docker Nacos。`9848` 是 Nacos 客户端 gRPC 端口，通常为 `8848 + 1000`。

### PostgreSQL 连接获取失败

如果出现：

```text
too many clients already
CannotGetJdbcConnectionException
SocketTimeoutException: Read timed out
```

优先处理方式：

1. 执行 `./scripts/stop-microservices.sh` 停止旧 Java 进程。
2. 重新执行 `./scripts/start-microservices.sh`。
3. 如果只演示药房、检查、门诊链路，可以只保留 `backend-service`、`gateway-service`、`pharmacy-service`、`inspection-service`、`outpatient-service`，减少数据库连接占用。

### Codex 环境脚本退出后端口不可访问

在 Codex 工具环境中，后台 Java 子进程可能随脚本结束被清理。这属于工具环境限制，不代表本地终端启动失败。正式演示建议在本地终端运行启动脚本。

