# 微服务设计文档

本文档是当前微服务架构的统一设计说明，整合自网关阶段说明、第二阶段拆分路线和后续测试结论。当前目标是形成可演示、可启动、可通过网关访问的微服务架构，而不是一次性完成生产级全链路解耦。

## 1. 总体架构

当前架构采用：

- Spring Boot 多模块服务。
- Spring Cloud Gateway 作为统一入口。
- Nacos 作为服务注册发现。
- Redis、Kafka、MinIO 作为基础组件。
- PostgreSQL 仍为共享业务数据库。

整体访问路径：

```text
Frontend / API Tool
  -> gateway-service:10010
     -> backend-service
     -> file-service
     -> patient-service
     -> doctor-service
     -> registration-service
     -> pharmacy-service
     -> inspection-service
     -> outpatient-service
```

支付接口当前仍由 `backend-service` 承接，独立 `payment-service` 作为基础拆分成果保留，暂不作为默认演示链路入口。

## 2. 拆分原则

本项目微服务拆分遵循以下原则：

1. 先保证能独立启动、注册到 Nacos、经网关访问。
2. 优先拆低耦合或边界清晰的业务域。
3. 共享数据库先保留，避免在实习项目中引入过多跨库事务复杂度。
4. 对跨服务事件强依赖的链路先保守处理。
5. 支付成功、支付超时、快速挂号等最终一致性链路后续再专项改造。

每拆一个服务的固定步骤：

1. 新建独立 Spring Boot 模块。
2. 设置独立 `spring.application.name` 和端口。
3. 接入 Nacos Discovery，`server-addr=127.0.0.1:8848`。
4. 复制该业务域最小必需的 Controller、Service、Mapper、Entity、DTO、VO。
5. 复制通用返回、异常处理、安全上下文等必要能力。
6. 启动并确认 Nacos 有健康实例。
7. 修改网关领域路由。
8. 通过网关访问接口。

## 3. 服务边界

| 业务域 | 服务 | 路径 | 当前状态 |
| --- | --- | --- | --- |
| 认证与系统 | `backend-service` | `/api/auth/**`, `/api/system/**` | 保留在后端 |
| 文件 | `file-service` | `/api/files/**` | 已拆分并验证 |
| 患者 | `patient-service` | `/api/patients/**` | 已拆分并验证 |
| 医生科室 | `doctor-service` | `/api/doctors/**`, `/api/departments/**` | 已拆分并验证 |
| 排班 | `registration-service` | `/api/schedules/**` | 已拆分并验证 |
| 挂号队列 | `registration-service` | `/api/registrations/**`, `/api/queue/**` | 已拆分并验证主干读接口 |
| 支付 | `backend-service` | `/api/payment/**` | 默认不切到 `payment-service` |
| 药房处方 | `pharmacy-service` | `/api/drugs/**`, `/api/prescriptions/**`, `/api/pharmacy/**` | 已拆分并验证读接口 |
| 检查检验处置 | `inspection-service` | 检查、检验、处置路径 | 已拆分并验证读接口 |
| CT 分析 | `backend-service` | `/api/ct-analysis/**` | 暂不拆分 |
| 门诊 | `outpatient-service` | `/api/outpatient/**` | 已拆分并验证读接口 |
| AI | `backend-service` | `/api/ai/**` | 暂不拆分 |

## 4. 网关路由设计

网关路由按真实控制器前缀划分业务域。

| 路由分组 | 路径 | 目标服务 |
| --- | --- | --- |
| 认证与系统 | `/api/auth/**`, `/api/system/**` | `lb://backend-service` |
| 患者 | `/api/patients/**` | `${PATIENT_SERVICE_URI:lb://patient-service}` |
| 医生科室 | `/api/doctors/**`, `/api/departments/**` | `${DOCTOR_SERVICE_URI:lb://doctor-service}` |
| 排班 | `/api/schedules/**` | `${REGISTRATION_SERVICE_URI:lb://registration-service}` |
| 挂号队列 | `/api/registrations/**`, `/api/queue/**` | `${REGISTRATION_SERVICE_URI:lb://registration-service}` |
| 文件 | `/api/files/**` | `${FILE_SERVICE_URI:lb://file-service}` |
| 药房处方 | `/api/drugs/**`, `/api/prescriptions/**`, `/api/pharmacy/**` | `${PHARMACY_SERVICE_URI:lb://pharmacy-service}` |
| 检查检验处置 | `/api/check-requests/**`, `/api/check-results/**`, `/api/inspection-requests/**`, `/api/inspection-results/**`, `/api/disposal-requests/**` | `${INSPECTION_SERVICE_URI:lb://inspection-service}` |
| 门诊 | `/api/outpatient/**` | `${OUTPATIENT_SERVICE_URI:lb://outpatient-service}` |
| CT 分析 | `/api/ct-analysis/**` | `lb://backend-service` |
| 支付 | `/api/payment/**` | `${PAYMENT_SERVICE_URI:lb://backend-service}` |
| AI | `/api/ai/**` | `lb://backend-service` |

环境变量形式保留了回退能力。例如需要临时把患者路由回退到后端，可以启动网关时设置：

```bash
PATIENT_SERVICE_URI=lb://backend-service
```

## 5. 安全设计

网关负责 JWT 前置校验：

- 校验签名。
- 校验过期时间。
- 校验 issuer。
- 将用户信息转换为下游请求头。

下游服务通过网关头读取用户上下文：

```text
X-User-Id
X-Username
X-User-Roles
X-User-Type
X-Biz-Id
```

网关放行：

- `/api/auth/login`
- `/api/auth/register`
- `/api/auth/refresh`
- `/api/auth/logout`
- `/v3/api-docs/**`
- `/swagger-ui/**`
- `/swagger-ui.html`
- `/doc.html`
- `/webjars/**`
- `/actuator/**`

其他接口必须携带 `Authorization: Bearer <accessToken>`。

## 6. 领域服务设计要点

### backend-service

保留认证、系统、支付、CT 分析、AI 和部分后端兜底能力。当前支付仍走这里，保证实习演示中的支付闭环不受跨 JVM 事件影响。

### file-service

主要依赖 MinIO 和 `file_record` 表。该服务与核心事务链路耦合低，适合作为早期拆分服务。

### patient-service

患者服务当前保持同库，可以继续访问系统账号和角色相关表。后续如果继续拆 `auth-service` 或 `system-service`，患者建档同步账号逻辑需要改为服务间接口。

### doctor-service

医生和科室作为独立领域。排班虽然业务上依赖医生，但代码在 `registration` 包中，已跟随 `registration-service` 拆分。

### registration-service

负责排班、挂号、队列。该服务依赖 Redis Lua 限流、Redisson 锁、Kafka、本地消息表、号源库存和支付状态，是当前业务中风险最高的拆分域。

当前主干查询和排班管理已验证；快速挂号 `/api/registrations/quick` 后续需要专项测试。

### payment-service

已经完成基础拆分和编译验证，但默认不承接 `/api/payment/**`。原因是支付成功和支付超时当前通过同 JVM Spring 事件通知挂号、检查检验、药房等监听器；拆成独立 JVM 后事件不会跨服务传播。

后续要正式切流，需要将支付事件改造为：

- Kafka 消息；或
- 显式服务间接口调用。

### pharmacy-service

负责药品、处方、发药。业务包与单体 `backend-service` 对应包保持一致，已通过网关读接口验证。支付联动仍等待支付事件改造。

### inspection-service

负责检查、检验、处置接口。CT 分析暂时仍在 `backend-service`，不纳入本轮 `inspection-service` 边界。

### outpatient-service

负责门诊病历接口。独立服务中保留患者访问病历详情所需的 `canAccessMedicalRecord` 判断，不复制完整后端安全层，只保留当前业务实际使用的访问辅助能力。

## 7. 当前保留在后端的能力

| 能力 | 保留原因 |
| --- | --- |
| 认证与系统 | 账户、角色、权限仍是全局基础能力 |
| 支付默认链路 | 支付事件尚未跨服务改造 |
| CT 分析 | 依赖 MinIO、Python 脚本和模型文件，拆分风险较高 |
| AI | 依赖外部 AI 流程和后端上下文，暂不拆分 |

## 8. 后续演进方向

1. 将支付成功和支付超时事件改造为 Kafka 或显式服务间接口。
2. 对 `/api/registrations/quick` 做专项测试，覆盖库存、Kafka、本地消息表和补偿。
3. 后续再考虑拆 `auth-service`、`system-service`、`ct-service`、`ai-service`。
4. 如果从实习演示走向上线，需要进一步拆分数据库边界和事务补偿机制。

